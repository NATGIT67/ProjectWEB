# Production Environment Variables
# These must be configured in Vercel Dashboard

NODE_ENV=production
PORT=3000

# Database Configuration (Must be set in Vercel Dashboard)
# For production, use a managed database service like:
# - PlanetScale (MySQL)
# - Supabase (PostgreSQL)
# - AWS RDS
# - Railway

DB_HOST=your_production_db_host
DB_USER=your_production_db_user
DB_PASSWORD=your_production_db_password
DB_NAME=easyrice_db

JWT_SECRET=your_production_jwt_secret_key_change_this
